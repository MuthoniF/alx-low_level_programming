#include "main.h"

/**
 * _isdigit - checks for a numeric character
 * @c: integer to check
 * Return: 1 if c is a numeric character, 0 if its not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
